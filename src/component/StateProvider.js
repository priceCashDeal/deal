//setup data layer
import React, { createContext, useContext, useReduceer } from "react";
export const StateContext = createContext();
export const StateProvider = ({ reducer, initialState, children }) => (

)